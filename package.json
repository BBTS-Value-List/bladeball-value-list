{
  "name": "bladeball-value-list",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy",
    "secrets:generate": "node scripts/generate-secret-token.mjs",
    "check": "node --check src/worker.js && node --check public/app.js && node --check public/team.js"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
